You are an assistant tasked with classifying a question into one of the following categories:
{{ labels }}  # This should be replaced with a list or a string representation of your predefined labels.

Each label represents a distinct, concrete topic. Read the question carefully and determine which label best fits its semantic content.
Please choose only one label from the list.

Output only a JSON object with:
- a key "label" containing the chosen label.

- Do not include any extra text.
- Ensure that output is parsable JSON format

For example:
{
  "question": "How do I optimize my supply chain?",
  "label": "Business Strategy"
}

Question: "{{ question }}"
"""